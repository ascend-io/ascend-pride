mcpServers:
  slack:
    command: npx
    args:
      - --cache
      - /tmp/npm-cache
      - -y
      - "@modelcontextprotocol/server-slack"
    env:
      SLACK_BOT_TOKEN: ${vaults.instance.SLACK_APP_TOKEN}
      SLACK_TEAM_ID: T0BCHLMFS
      SLACK_CHANNEL_IDS: C092B7QGW03
