---
alwaysApply: true
description:
globs: []
keywords: []
---

# URL Rules

## Deployments

When generating URLs for flow runs, they should follow the pattern of:

https://[hostname]/project/[project_name]/[deployment_name]/flows/[flow_name]/[run_name]

- `run_name` always starts with `fr-`
